# Backend (Express + MongoDB)

This folder contains the backend server. Run these commands inside `backend/`.

Copy `.env` from `.env.example` and set `MONGODB_URI` and `JWT_SECRET`.

Install and start:

```bash
cd backend
npm install
npm run dev
```

API is available at http://localhost:3000 by default.
